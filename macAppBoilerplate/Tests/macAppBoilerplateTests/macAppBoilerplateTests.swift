import XCTest
@testable import macAppBoilerplate

final class macAppBoilerplateTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(macAppBoilerplate().text, "Hello, World!")
    }
}
